/*
 * NoOffer Messages
 *
 * This contains all the text for the NoOffer component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.NoOffer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NoOffer component!',
  },
});
