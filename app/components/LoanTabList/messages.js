/*
 * LoanTabList Messages
 *
 * This contains all the text for the LoanTabList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LoanTabList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LoanTabList component!',
  },
});
