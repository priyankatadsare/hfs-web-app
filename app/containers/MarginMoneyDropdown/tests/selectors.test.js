// import { selectMarginMoneyDropdownDomain } from '../selectors';

describe('selectMarginMoneyDropdownDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
