// import { selectConstitutionDropdownDomain } from '../selectors';

describe('selectConstitutionDropdownDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
