/*
 * ConstitutionDropdown Messages
 *
 * This contains all the text for the ConstitutionDropdown container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ConstitutionDropdown';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ConstitutionDropdown container!',
  },
});
