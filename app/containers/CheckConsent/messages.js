/*
 * CheckConsent Messages
 *
 * This contains all the text for the CheckConsent container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CheckConsent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CheckConsent container!',
  },
});
