// import { selectCheckConsentDomain } from '../selectors';

describe('selectCheckConsentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
