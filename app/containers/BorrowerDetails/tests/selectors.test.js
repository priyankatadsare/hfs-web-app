// import { selectBorrowerDetailsDomain } from '../selectors';

describe('selectBorrowerDetailsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
