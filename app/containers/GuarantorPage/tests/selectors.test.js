// import { selectGuarantorPageDomain } from '../selectors';

describe('selectGuarantorPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
