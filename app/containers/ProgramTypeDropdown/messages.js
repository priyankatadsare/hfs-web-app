/*
 * ProgramTypeDropdown Messages
 *
 * This contains all the text for the ProgramTypeDropdown container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProgramTypeDropdown';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProgramTypeDropdown container!',
  },
});
