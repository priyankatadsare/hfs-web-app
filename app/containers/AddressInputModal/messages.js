/*
 * AddressInputModal Messages
 *
 * This contains all the text for the AddressInputModal container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddressInputModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddressInputModal container!',
  },
});
