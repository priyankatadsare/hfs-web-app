// import { selectAddressInputModalDomain } from '../selectors';

describe('selectAddressInputModalDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
