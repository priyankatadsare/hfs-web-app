// import { selectRelationshipDropDownDomain } from '../selectors';

describe('selectRelationshipDropDownDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
