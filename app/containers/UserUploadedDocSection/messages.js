/*
 * UserUploadedDocSection Messages
 *
 * This contains all the text for the UserUploadedDocSection container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserUploadedDocSection';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserUploadedDocSection container!',
  },
});
