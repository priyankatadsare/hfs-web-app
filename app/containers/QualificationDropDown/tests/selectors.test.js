// import { selectQualificationDropDownDomain } from '../selectors';

describe('selectQualificationDropDownDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
