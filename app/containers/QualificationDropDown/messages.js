/*
 * QualificationDropDown Messages
 *
 * This contains all the text for the QualificationDropDown container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.QualificationDropDown';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the QualificationDropDown container!',
  },
});
