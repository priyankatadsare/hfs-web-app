/*
 * BorrowerDetailsPendingSection Messages
 *
 * This contains all the text for the BorrowerDetailsPendingSection container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BorrowerDetailsPendingSection';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BorrowerDetailsPendingSection container!',
  },
});
