// import { selectBorrowerDetailsPendingSectionDomain } from '../selectors';

describe('selectBorrowerDetailsPendingSectionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
