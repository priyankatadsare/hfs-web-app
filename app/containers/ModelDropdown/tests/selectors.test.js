// import { selectModelDropdownDomain } from '../selectors';

describe('selectModelDropdownDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
