/*
 * ModelDropdown Messages
 *
 * This contains all the text for the ModelDropdown container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ModelDropdown';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ModelDropdown container!',
  },
});
