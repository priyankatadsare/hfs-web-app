/*
 * AssetConditionDropdown Messages
 *
 * This contains all the text for the AssetConditionDropdown container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AssetConditionDropdown';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AssetConditionDropdown container!',
  },
});
