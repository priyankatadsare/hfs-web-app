// import { selectBorrowerTypeDomain } from '../selectors';

describe('selectBorrowerTypeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
