/*
 * BorrowerType Messages
 *
 * This contains all the text for the BorrowerType container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BorrowerType';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BorrowerType container!',
  },
});
