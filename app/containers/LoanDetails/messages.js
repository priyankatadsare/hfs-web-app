/*
 * LoanDetails Messages
 *
 * This contains all the text for the LoanDetails container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LoanDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LoanDetails container!',
  },
});
