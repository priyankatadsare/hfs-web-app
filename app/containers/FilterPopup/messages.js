/*
 * FilterPopup Messages
 *
 * This contains all the text for the FilterPopup container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FilterPopup';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FilterPopup container!',
  },
});
