/*
 * TenureDropdown Messages
 *
 * This contains all the text for the TenureDropdown container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TenureDropdown';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TenureDropdown container!',
  },
});
