/*
 * AssetTypeDropdown Messages
 *
 * This contains all the text for the AssetTypeDropdown container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AssetTypeDropdown';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AssetTypeDropdown container!',
  },
});
