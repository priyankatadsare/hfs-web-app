// import { selectDocumentUploadDomain } from '../selectors';

describe('selectDocumentUploadDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
