/*
 * SetUpTypeDropdownDropdown Messages
 *
 * This contains all the text for the SetUpTypeDropdownDropdown container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SetUpTypeDropdownDropdown';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SetUpTypeDropdownDropdown container!',
  },
});
