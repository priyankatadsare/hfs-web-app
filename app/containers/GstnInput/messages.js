/*
 * GstnInput Messages
 *
 * This contains all the text for the GstnInput container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GstnInput';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GstnInput container!',
  },
});
