/*
 * WorkExperienceDropdownDropdown Messages
 *
 * This contains all the text for the WorkExperienceDropdownDropdown container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WorkExperienceDropdownDropdown';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WorkExperienceDropdownDropdown container!',
  },
});
