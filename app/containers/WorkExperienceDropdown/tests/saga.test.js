/**
 * Test sagas
 */

/* eslint-disable redux-saga/yield-effects */
// import { take, call, put, select } from 'redux-saga/effects';
// import WorkExperienceDropdownDropdownSaga from '../saga';

// const generator = WorkExperienceDropdownDropdownSaga();

describe('WorkExperienceDropdownDropdownSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
