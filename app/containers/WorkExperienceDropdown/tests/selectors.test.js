// import { selectWorkExperienceDropdownDropdownDomain } from '../selectors';

describe('selectWorkExperienceDropdownDropdownDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
