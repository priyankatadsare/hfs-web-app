// import { selectSupplierDropdownDomain } from '../selectors';

describe('selectSupplierDropdownDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
