/*
 * SendEmailConsent Messages
 *
 * This contains all the text for the SendEmailConsent container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SendEmailConsent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SendEmailConsent container!',
  },
});
