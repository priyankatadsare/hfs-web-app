// import { selectAddressTypeDropdownDomain } from '../selectors';

describe('selectAddressTypeDropdownDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
