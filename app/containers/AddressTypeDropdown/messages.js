/*
 * AddressTypeDropdown Messages
 *
 * This contains all the text for the AddressTypeDropdown container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddressTypeDropdown';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddressTypeDropdown container!',
  },
});
