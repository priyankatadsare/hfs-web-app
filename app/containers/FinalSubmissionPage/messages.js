/*
 * FinalSubmissionPage Messages
 *
 * This contains all the text for the FinalSubmissionPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FinalSubmissionPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FinalSubmissionPage container!',
  },
});
