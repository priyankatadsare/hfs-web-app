/*
 * ManufactureDropdown Messages
 *
 * This contains all the text for the ManufactureDropdown container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ManufactureDropdown';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ManufactureDropdown container!',
  },
});
