// import { selectLoanApplicationsDomain } from '../selectors';

describe('selectLoanApplicationsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
