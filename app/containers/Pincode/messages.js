/*
 * Pincode Messages
 *
 * This contains all the text for the Pincode container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Pincode';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Pincode container!',
  },
});
