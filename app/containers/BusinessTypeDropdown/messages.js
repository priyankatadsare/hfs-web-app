/*
 * BusinessProfileDropdown Messages
 *
 * This contains all the text for the BusinessProfileDropdown container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BusinessProfileDropdown';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BusinessProfileDropdown container!',
  },
});
